# 假设登记表（Assumptions Register）

> 制度：**任何未经验证的假设，禁止在代码中依赖它**。每条假设有编号/验证方法/状态。
> 状态：✅已验证 / ⚠️部分验证 / ⏳待验证 / ❌已被推翻（须走 ADR）。

## 数据源（GitHub Search API）

| # | 假设 | 验证方法 | 状态 | 证据/结论 |
|---|---|---|---|---|
| A1 | `created:>日期 archived:false sort=stars` 语法可用 | 预演 S1（curl 实测） | ✅ | total 269,818，返回 30 条正常 |
| A2 | 中文 NOT 排除词生效 | 预演 S1 + M2 生产查询 | ✅ | 预演：total 减少 120 条；M2：4 词查询稳定返回 45 条 |
| A3 | 英文排除词生效（post-filter 层） | M2 单测 + 抽样 | ✅ | 单词边界匹配：`quant` 不误杀 `quantum`（M2 实测 #20 存活） |
| A4 | 排除后无量化项目漏网 | 人工抽样 | ⚠️ 1/3 | M2 抽样 25 条 0 漏网；连续 3 天完成才算过 |
| A5 | 响应体积 ~4KB，JSON.parse CPU <2ms | 预演 S1 + M2 全链路 | ⚠️ | 实测 3,651 字符；M2 抓取 45 条全程 2 秒（含网络）；CPU 数值 M6-S3 实测 |
| A6 | 未认证调用 60 次/小时足够日常 | 官方文档 + 部署后观察 | ✅风险确认 | **M5 实测：未认证 GraphQL 403 配额耗尽（共享 IP）**。预案通道（GITHUB_TOKEN）实测有效；生产必须配置只读 PAT（ADR-0004），此为部署硬前置 |
| A7 | 抓取窗口通常 ≥25 条 | M2 实测 + 05:00 真实时点观察 | ⚠️ | 18:54 实测 45 条足额；05:00 时点待部署后观察 |
| A8 | **查询层 NOT 数无限制** | M2 生产运行（422 实测） | ❌ | **被推翻**：GitHub 硬限制单查询 ≤5 个 AND/OR/NOT（超过 422）。对策：查询层仅 4 个 NOT + 全量 24 词 post-filter + 回归守卫单测（已落地） |

## 运行环境（Cloudflare）

| # | 假设 | 验证方法 | 状态 | 证据/结论 |
|---|---|---|---|---|
| E1 | 免费计划限额数字（10 万请求/10ms CPU/5 cron/D1 5GB） | 官方文档实测 | ✅ | 已核对 [官方 Limits 文档](https://developers.cloudflare.com/workers/platform/limits/) |
| E2 | wrangler CLI 可用 | 预演 S2 → M1 安装 | ✅ | 已装 wrangler **4.122.0**（v3 已过期被官方警告；npm 11 需显式批准 workerd/esbuild/sharp 安装脚本） |
| E3 | CF 账号可登录（用户已有账号） | M6：wrangler login 浏览器授权 | ⏳ | 需用户配合一次浏览器授权 |
| E4 | D1 batch 计为 1 次子请求 | M2 实测（子请求计数日志） | ⏳ | 若不成立，改分批串行（每批 ≤50） |
| E5 | cron 墙钟 ≥15min（≥1h 间隔），AI 分批有足够时间 | 官方文档 | ✅ | 按 22s 截止 + 分批设计，余量大 |

## AI 供应商

| # | 假设 | 验证方法 | 状态 | 证据/结论 |
|---|---|---|---|---|
| I1 | openai-compatible 协议适配器可覆盖多数厂商 | M4 mock 集成 + 真实 key 待验 | ⚠️ | mock 全链路 ✅（测试/拉模型/生成）；真实厂商待 M6 用户配置后点「测试」 |
| I2 | gemini-native 协议直连可用 | M4 mock 集成 + 真实 key 待验 | ⚠️ | 协议代码已实现，真实连通待 M6 |
| I3 | 模型列表接口存在（openai /models、gemini /models） | M4 mock 实测 + 失败降级手动填 | ✅ | mock ✅；真实厂商不支持时 UI 已降级提示手动输入 |
| I4 | AI 等待时间不计 CPU | 官方文档明示 | ✅ | "Waiting on network requests does not count toward CPU time" |

## 前端与安全

| # | 假设 | 验证方法 | 状态 | 证据/结论 |
|---|---|---|---|---|
| F1 | 30s localStorage 缓存 + Token 模式可用 | 旧方案已验证 | ✅ | 沿用 |
| F2 | 主题防白闪 inline script 方案 | 旧方案已验证 | ✅ | 沿用 |
| F3 | 单用户场景请求量 <1000/天 | 部署后观察 1 周 | ⏳ | /debug 接口记录 |

## 变更规则

- 新假设 → 追加编号 + 验证方法；推翻假设 → 标 ❌ + 在 ADR 记录新决策。
- 每个里程碑验收清单必须包含"本阶段假设的验证结果写回本表"这一项。
