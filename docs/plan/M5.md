# M5 计划与验收（L1 追踪系统：收藏即追踪）

> 五步法记录：设计 → 实现 → 自检 → 本地验证 → 用户验收

## 任务清单

- [x] 任务1 GraphQL 适配器：别名批量查询（r0..rN，无需 node id）+ 空仓库容错 + 50 仓库/请求分片预留
- [x] 任务2 tracker 服务：快照更新（含 latest_release/pushed_at）+ star_history INSERT OR REPLACE（同日幂等）+ 审计
- [x] 任务3 API：GET /favs?history=1（近 30 天曲线一次带回）+ admin/trigger tracker 任务 + scheduled 21:00
- [x] 任务4 前端：收藏页追踪视图（★ 当前值 / 自收藏涨跌徽章 / SVG 星标曲线 / 🔔 release 徽章 / 手动同步按钮）
- [x] 任务5 单测：43 个全绿（GraphQL 查询构建/解析 + sparkPath 4 例）
- [x] 任务6 集成验证（真实 GitHub GraphQL，见下）
- [ ] 用户验收（等待中）

## 自检结果（npm run check）

43 个单测全部通过，exit 0 ✅

## 本地集成验证结果（真实 GitHub GraphQL）

| # | 验证点 | 预期 | 实际 |
|---|---|---|---|
| 1 | 收藏 2 仓库 → 触发追踪 | 同步成功 | ✅ ok=2 fail=0 |
| 2 | 快照更新 | 真实星标 + release | ✅ ★15 / ★139 + pushed_at（Bigfish 已从抓取时 ★110 涨到 ★139——追踪价值实证） |
| 3 | star_history 时间序列 | 每仓库每天 1 行 | ✅ 2 行 |
| 4 | 同日幂等（REPLACE） | 重复同步行数不变 | ✅ 仍为 2 行 |
| 5 | 审计 | tracker_sync 行 status=done | ✅ |

## 本阶段真实故障与修复（ADR-0004 预言成真）

| # | 发现 | 根因 | 修复 |
|---|---|---|---|
| 1 | GraphQL 403 rate limit exceeded（IP 120.204.x.x） | **未认证配额被耗尽**——ADR-0004 预警的共享配额风险真实发生 | 按预案：挂 token。本地 dev 用 gh token（写入 gitignored .dev.vars，不回显）；**生产必须用用户自建的只读 PAT** |

结论：A6 假设从"⚠️风险"升级为"✅已验证的风险"，预案通道（token）实测有效。

## 本阶段产出

- `src/adapters/github-graphql.js` · `src/services/tracker.js`
- `src/routes/favs.js`（history 支持）· `src/routes/admin.js`（tracker 任务）· `src/index.js`（21:00 调度）
- `public/assets/pages/favs.js`（追踪视图）· `public/assets/app.js`（sparkPath 纯函数）
- `tests/tracker.test.js`

## 遗留（非阻塞）

- 曲线至少需要 2 天才成形（M6 部署后隔天可见）
- 未认证配额耗尽事件已记录 PLAYBOOK（检测/响应/回滚路径与 #1 同构）
