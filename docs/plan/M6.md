# M6 计划与验收（部署上线 + 端到端 + 演练#3）

> 五步法记录：设计 → 实现 → 自检 → 本地验证 → 用户验收

## 部署信息

| 项 | 值 |
|---|---|
| 线上地址 | https://star-radar.zal-pc-remote.workers.dev |
| CF 账号 | 2961737771@qq.com（OAuth，无需再登录） |
| D1 | star-radar-db（WNAM 区域，database_id 已写入 wrangler.toml） |
| Cron | 3 触发器：UTC 21:00 / 22:00 / 13:00 = 北京 05:00 / 06:00 / 21:00 |
| ADMIN_TOKEN | 已设 secret；本地副本在 `admin-token.txt`（gitignored，勿提交） |
| 当前版本 | ec116078-4533-485b-a202-eff677f604d4 |

## 部署步骤（已执行）

1. ✅ `wrangler d1 create star-radar-db` → database_id 自动写回 wrangler.toml
2. ✅ `npm run db:init:remote` → 6 张表远程建好
3. ✅ 生成随机 ADMIN_TOKEN → `wrangler secret put ADMIN_TOKEN`（不回显，副本存本地 gitignored 文件）
4. ✅ `npm run deploy` → workers.dev 上线 + 3 cron 挂载

## 端到端验收结果（远程实测）

| # | 验证点 | 预期 | 实际 |
|---|---|---|---|
| 1 | 五页面 + 4 静态资源 | 200 | ✅ 9/9 |
| 2 | 远程抓取（CF 边缘出口 IP） | 25 条入库 | ✅ ok=25（1 秒完成，无 403） |
| 3 | dates / projects | 今日 25 条 | ✅ |
| 4 | 收藏流（远程 D1） | 增查删闭环 | ✅ |
| 5 | 排行榜 7d | 10 条正确形状 | ✅（橙皮书已从 ★404 涨到 ★449） |
| 6 | AI 无供应商错误路径 | 502 + 中文引导 | ✅ |
| 7 | providers CRUD + 脱敏 | ***masked*** | ✅ |
| 8 | debug 审计 | 抓取行 status=done | ✅ |
| 9 | 回滚演练（桌面演练 #3） | 坏版本 → 秒级回滚 | ✅ SUCCESS，100% 流量恢复 |

## 本阶段发现与修复

| # | 发现 | 根因 | 修复 |
|---|---|---|---|
| 1 | 线上首页/静态资源全部 404（本地 dev 正常） | **wrangler v4 生产环境必须显式调用 `env.ASSETS.fetch()`** 兜底未命中路由 | index.js 增加 ASSETS 兜底后重新部署，9 项资源全 200 |
| 2 | 回滚演练第 2 步"坏版本标记"未即时可见 | 静态资源全球 CDN 传播有数秒延迟（2s 等待不足） | 非产品问题；演练核心（rollback 机制）验证通过，记录传播延迟经验值 |

## S3 CPU 实测（结论）

- 全量 API + 远程抓取 25 条全部成功，**零 Error 1102**，抓取 1-2 秒完成 → 10ms CPU 预算未超限 ✅
- 精确 CPU 数值需 CF Dashboard（Metrics → Invocation Statuses）查看，留给用户验收时确认

## PLAYBOOK 上线前检查表对照

- [x] 上一版本 tag/Version ID 存在，可随时 rollback（实测演练通过）
- [x] ADMIN_TOKEN 已设置（secret，副本本地 gitignored）
- [ ] GitHub 只读 PAT 配置（**未做，见下**）
- [x] /debug 显示 cron_runs、行数
- [ ] 手机 + 电脑完整浏览闭环（待用户验收）
- [x] 手动触发 /admin/trigger 验证补抓链路
- [ ] CHANGELOG 记录（M7 随 git 提交补）

## 用户待办（3 项，全部一次完成）

1. **只读 PAT**（重要）：GitHub → Settings → Developer settings → Fine-grained tokens → 仅勾选 Public Repositories 只读 → 生成后运行
   `npx wrangler secret put GITHUB_TOKEN`（粘贴 PAT）。不配也可用（今天实测未认证正常），但 A6 风险已实证，强烈建议配置。
2. **AI key**：打开线上地址 → 设置页 → 添加供应商（选预设 → 贴 key → 测试 → 启用 → 保存）→ 日报详情点「立即生成」验收 AI 质量。
3. **浏览器走查**：手机 + 电脑各走一遍 日报/收藏/排行/设置。

## 验收结论

M6 核心目标达成：**0 元成本上线运行**，免费额度全部符合（请求/CPU/D1/cron），
首次自动抓取将于今晚 UTC 21:00（北京明晨 05:00）由 cron 执行。
