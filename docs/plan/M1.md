# M1 计划与验收（L0 骨架）

> 五步法记录：设计 → 实现 → 自检 → 本地验证 → 用户验收

## 任务清单

- [x] 任务1 环境检查：Node v24.18.0 / npm 11.16.0（假设登记表 E2 更新）
- [x] 任务2 安装 wrangler：v3.114.17 → 升级 **v4.122.0**（官方警告 v3 已过期；镜像源最新为 4.122.0）
- [x] 任务3 项目骨架：package.json / wrangler.toml（3 cron 触发器已声明）/ .gitignore / .dev.vars(.example)
- [x] 任务4 权威 schema：migrations/0001_init.sql（6 张表 + 索引，对齐 ARCHITECTURE §5）
- [x] 任务5 分层代码：src/index.js（路由注册）/ lib/http.js（统一结构+安全头）/ lib/auth.js（常数时间比较）/ routes/dates.js / routes/debug.js
- [x] 任务6 单测：tests/http.test.js + tests/auth.test.js（8 用例）
- [x] 任务7 本地 D1 建表：npm run db:init --local 成功
- [x] 任务8 本地冒烟：wrangler dev 起服务，6 路请求验证
- [ ] S3 CPU 实测：需远程环境，M6 首次部署时执行，结果写回 ASSUMPTIONS A5
- [ ] 用户验收（等待中）

## 自检结果（npm run check）

- 5 个 src 文件语法通过；8 个单测全部通过，exit 0 ✅

## 本地验证结果（wrangler dev + 6 路冒烟）

| # | 请求 | 预期 | 实际 |
|---|---|---|---|
| 1 | GET /api/v1/dates | `{"ok":true,"data":[]}` | ✅ 一致 |
| 2 | GET /api/v1/debug 无 Token | 401 unauthorized | ✅ 一致 |
| 3 | GET /api/v1/debug 带 Token | ok + 表计数全 0 | ✅ 一致 |
| 4 | GET / 静态页 | 200 占位页 | ✅ 一致 |
| 5 | OPTIONS 预检 | 204 | ✅ 一致 |
| 6 | GET 未知路由 | 404 统一错误结构 | ✅ 一致 |

## 本阶段发现与修复（预演价值的实证）

| # | 发现 | 修复 |
|---|---|---|
| 1 | check.js 注释内 `*/` 提前终止块注释 → 语法错误 | 改写注释 |
| 2 | `node --test tests/` 在 Windows 被当模块解析 | 改为程序化发现测试文件显式传参 |
| 3 | npm 11 allow-scripts 拦截 workerd/esbuild/sharp 安装脚本 | package.json 显式批准 + rebuild |
| 4 | npmjs 直连超时 | 镜像源 registry.npmmirror.com |
| 5 | wrangler 3.114.17 已过期（compat date 2026-08-01 被回退到 2025-07-18） | 升级 4.122.0，警告消除 |
| 6 | ^4.123.0 不存在于镜像源 | 用镜像实测最新版 ^4.122.0 |

## 本阶段产出

- `package.json` `wrangler.toml` `.gitignore` `.dev.vars.example`
- `migrations/0001_init.sql`（权威 schema）
- `src/index.js` `src/lib/http.js` `src/lib/auth.js` `src/routes/dates.js` `src/routes/debug.js`
- `tests/http.test.js` `tests/auth.test.js` `scripts/check.js`
- `public/index.html`（M3 占位）

## 遗留（非阻塞）

- S3 CPU 实测：本地 workerd 无法反映云端计费，M6 部署后执行
- M2 可用 v4 特性：`curl http://127.0.0.1:8787/cdn-cgi/local/scheduled` 本地触发 cron（已记录）
